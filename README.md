# Todo App

A clean, minimal todo list app built with plain HTML, CSS, and JavaScript. No dependencies, no build step.

## Features

- Add, complete, and delete tasks
- Persists to `localStorage` — survives page refreshes
- "Clear completed" to bulk-remove done tasks

## Usage

Just open `index.html` in a browser. No server required.

## Structure

```
todo-app/
├── index.html   # Markup
├── style.css    # Styles
└── app.js       # Logic
```

Spartan Mule Team created this project

readme last updated 2026-04-24
